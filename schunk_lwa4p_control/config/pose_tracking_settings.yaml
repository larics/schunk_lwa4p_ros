#################################
# PID parameters for pose seeking
#################################

# Maximum value of error integral for all PID controllers
windup_limit: 0.1

# PID gains --> Increase gains even more, test it in simulation, CHECK REAL ROBOT SAFETY
x_proportional_gain: 50
y_proportional_gain: 50
z_proportional_gain: 50
x_integral_gain: 0.5
y_integral_gain: 0.5
z_integral_gain: 0.5
x_derivative_gain: 0.01
y_derivative_gain: 0.01
z_derivative_gain: 0.01

angular_proportional_gain: 15
angular_integral_gain: 0.0
angular_derivative_gain: 0.0